# SFS_MAIN Directory Cleanup Log

**Date:** 2025-11-15
**Performed by:** Claude Code (automated cleanup)

## Summary

Cleaned up `/data/gpfs/projects/punim2657/sfs_main/` directory from **29GB → ~12GB**, recovering approximately **17GB** of space.

## Actions Performed

### Phase 1: Parallel Branches (Saved ~16GB)
- **Removed:** 18 `parallel_branch_*/` directories
- **Reason:** Experimental branches no longer needed, code preserved in main `sfspreproc-docker/`
- **Size recovered:** ~16GB

### Phase 2: Old Results (Saved ~2GB)
- **Archived:** 27 result directories older than 60 days → `results_archive_old_20251115.tar.gz` (464MB compressed)
- **Moved:** 176 result directories (14-60 days old) → `results_archive_recent/`
- **Removed:** Old relaxed threshold tests (`results_relaxed_*`) - 174MB each
- **Kept:** Recent results from last 14 days

### Phase 3: Build Artifacts (Saved ~1.2GB)
- **Removed from `sfspreproc-docker/`:**
  - `Dataset/` (981MB) - sample data, not needed
  - `_deps/` (136MB) - CMake dependencies, can rebuild
  - `CMakeFiles/` - build cache
  - Old `results_*/` directories (14 × 5.6MB)

### Phase 4: Logs (Saved ~500MB)
- **Archived:** SLURM log files >30 days → `logs_archive/logs_archive_20251115.tar.gz`
- **Kept:** Recent logs (<30 days)

### Phase 5: Preprocessing Cleanup (Saved ~456MB)
- **Removed TPS backups:**
  - `archive_old_datasets/TPS_Complete_Dataset` (105M)
  - `archive_old_datasets/TPS_Dataset_20250829` (112M)
  - `archive_old_datasets/TPS_Dataset_20250831` (105M)
  - `archive_old_datasets/TPS_PREPROCESSING_BACKUP_20250830_1430` (32M)
  - `archive_old_datasets/TPS_Processing_Workspace` (102M)
  - Other TPS-related archives

## Current Directory Structure

```
sfs_main/                          ~12GB total
├── sfspreproc-docker/            73M (main assembly code)
├── sfs_legacy_temp/              ~500M (Tray-000 debugging)
├── sfspreproc.sif                815M (container image)
├── results_2025_XX_XX_XXXX/      ~1-2GB (recent results <14 days)
├── results_archive_recent/       ~1GB (results 14-60 days)
├── results_archive_old_*.tar.gz  464M (old results compressed)
├── logs_archive/*.tar.gz         ~100M (old logs compressed)
└── *.md                          Documentation files
```

## What Was Kept

### Essential Files
- `sfspreproc-docker/` - Main SFS++ assembly algorithms
- `sfs_legacy_temp/` - Legacy code for Tray-000 debugging
- `sfspreproc.sif` - Apptainer container (815MB)
- All `.md` documentation files
- All `.sbatch` scripts

### Recent Data
- Result directories from last 14 days
- SLURM logs from last 30 days
- Current build outputs

### Archives
- `results_archive_recent/` - Intermediate results (can delete if not needed)
- `results_archive_old_20251115.tar.gz` - Old results (can delete after verification)
- `logs_archive_20251115.tar.gz` - Old logs (can delete if not needed)

## Recovery Instructions

If archived data is needed:

```bash
# Restore old results
tar -xzf results_archive_old_20251115.tar.gz

# Restore old logs
cd logs_archive
tar -xzf logs_archive_20251115.tar.gz

# Rebuild sfspreproc-docker if needed
cd sfspreproc-docker
cmake . && make -j8
```

## Notes

- **Directory naming clarification:** `sfspreproc-docker/` is misleadingly named - it contains ASSEMBLY code, not preprocessing. See CLAUDE.md for details.
- **Parallel branches:** Removed as they were experimental copies. Code is preserved in main directory.
- **Build artifacts:** Can be regenerated by running cmake/make when needed.
- **TPS backups:** Old preprocessing test data removed from `sfs_preprocessing/archive_old_datasets/`.

## Recommendations

### Can Safely Delete (if disk space needed):
- `results_archive_recent/` after reviewing
- `results_archive_old_20251115.tar.gz` after verification
- `logs_archive_20251115.tar.gz` after verification

### Should Keep:
- `sfspreproc-docker/` (main code)
- `sfs_legacy_temp/` (debugging copy)
- `sfspreproc.sif` (container)
- Recent results (<14 days)
- Documentation (.md files)
